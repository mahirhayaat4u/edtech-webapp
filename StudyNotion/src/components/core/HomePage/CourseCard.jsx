import React from 'react'

const CourseCard = ({element,setCurrentCard,currentCard}) => {
  return (
    <div> 
       {/* {
        element.map((element,index)=>{
            return(
                <div key={index}>{element.setCurrentCard}</div>
            )
        })
       } */}
    </div>
  )
}

export default CourseCard